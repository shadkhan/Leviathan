{
  "name": "leviathan",
  "version": "0.1.0",
  "private": true,
  "description": "A JSON viewer that survives large files. Rust/WASM core, MV3 extension shell.",
  "license": "MIT OR Apache-2.0",
  "repository": "https://github.com/shadkhan/leviathan",
  "scripts": {
    "build": "pnpm -C packages/extension build",
    "build:wasm": "pnpm -C packages/extension build:wasm",
    "dev": "pnpm -C packages/extension dev",
    "typecheck": "pnpm -C packages/extension typecheck",
    "test": "pnpm -C packages/extension test",
    "smoke": "pnpm -C packages/extension smoke",
    "check": "cargo fmt --check && cargo clippy --workspace --all-targets -- -D warnings && cargo test --workspace && pnpm typecheck && pnpm test && pnpm build && pnpm smoke"
  },
  "packageManager": "pnpm@10.33.2"
}
